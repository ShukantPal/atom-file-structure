// @flow

/*::
import type { CSymbolTree } from '../types'

export type Builder<T> = {
  buildSymbolTree(code: string): T,
  buildView(view: any, tree: T): void,
  test(fileExtension: string, file: string): boolean
}
 */
